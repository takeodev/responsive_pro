name: responsive_plus
description: "Responsive Plus — Responsividade avançada para Flutter."
version: 1.0.1+1

repository: https://github.com/takeodev/responsive_plus
environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: false
  plugin:
    platforms:
      dart:
        pluginClass: ResponsivePlus